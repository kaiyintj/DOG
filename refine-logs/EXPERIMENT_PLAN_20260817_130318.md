---
plan_id: carla_aris_w15_20260817_130318
workflow: ARIS_W1.5_experiment_bridge
status: ready_for_execution_after_p0_preflight
language: zh
evaluation_type: simulation_only
orchestrator: wanshuiyin/Auto-claude-code-research-in-sleep
planning_model: gpt-5.6-sol
execution_model: gpt-5.6-luna
execution_reasoning_effort: max
backend: local
max_parallel_runs: 1
auto_deploy: true
sanity_first: true
wandb: false
code_sync: local
created: 2026-08-17T13:03:18+08:00
---

# CARLA 自主测试 ARIS W1.5 可执行计划

## 0. 执行结论与范围

本计划把 `docs/CARLA_AUTONOMOUS_TEST_PLAN.md` 转为 Auto-Claude 可依赖调度的 sanity → baseline → main → ablation 工件。只自动执行 P0 与 P1；P2 只估算、保持 `TODO (HOLD_P2)`。本轮规划不运行实验、不修改源码或正式 YAML。

执行顺序：

1. **sanity**：P0-A 环境/代码硬门，P0-C 30 帧双链路冒烟；
2. **baseline**：P0-B 旧数据回归，P0-D SegFormer 类别能力，P0-E 静态点级/体素基线；
3. **main**：P1-F Range/Density/View/Semantic 控制实验，P1-G Motion/offset，P1-I 多 seed 统计；
4. **ablation**：P1-H 同点、同 posterior、同 timestamp、同 GT support 的六组 VoxelMap 消融；
5. **P2 hold**：跨 Town/天气、CLIP、性能与长序列只报预算，绝不自动启动。

任何 P0 硬门失败，后续 P0/P1 标记 `NOT_RUN`。主线程已在非受限本地上下文确认 RTX 5070 8 GB、`nvidia-smi`、PyTorch CUDA 与 CARLA Python API 可用；执行模型仍必须把下面的 P0-BOOT/P0-A 作为第一条实际命令，记录当次资源状态后再继续。

## 1. 不变量

- 项目：`/home/yk/ws/src/semantic_mapping`；工作区：`/home/yk/ws`。
- 模型：`nvidia/segformer-b0-finetuned-cityscapes-1024-1024`，revision 固定为 `21b3847fae21ddee674abd31129307b6a1235bd9`，不得静默改版。
- 单 GPU 串行。CARLA 采集与 SegFormer 推理不并发。
- Semantic LiDAR 只作离线 GT。`electric_bicycle` 为 unsupported。
- Motion V2 只作 CARLA diagnostic；当前体素主门失败，禁止接入 runtime。
- 参数扫描从正式 YAML 复制副本到 `$AUTOTEST_ROOT/configs/` 后只改副本。
- 禁止提交、推送、删除旧数据、覆盖结果、`pkill`/`killall`；保护 `AGENTS.md` 记录的 dirty 基线。
- 失败后先读日志；不允许无改动重试。仅对日志证明的实验工具问题允许最多 2 次最小修复+复测。

## 2. 统一运行根与结构化协议

### P0 首条实际命令（P0-BOOT-001）

```bash
set -euo pipefail
cd /home/yk/ws/src/semantic_mapping
RUN_ID="$(date +%Y%m%d_%H%M%S)"
AUTOTEST_ROOT="/home/yk/ws/carla_benchmark_data/autotest_${RUN_ID}"
SEGFORMER_MODEL="nvidia/segformer-b0-finetuned-cityscapes-1024-1024"
SEGFORMER_REVISION="21b3847fae21ddee674abd31129307b6a1235bd9"
mkdir -p "$AUTOTEST_ROOT"/{logs,preflight,image,reliability,configs,summary,run_records}
printf '%s\n' "$AUTOTEST_ROOT" > /home/yk/ws/carla_benchmark_data/AUTOTEST_CURRENT.txt
printf 'export AUTOTEST_ROOT=%q\nexport SEGFORMER_MODEL=%q\nexport SEGFORMER_REVISION=%q\n' \
  "$AUTOTEST_ROOT" "$SEGFORMER_MODEL" "$SEGFORMER_REVISION" \
  > "$AUTOTEST_ROOT/run.env"
printf '{"plan_id":"carla_aris_w15_20260817_130318","status":"TODO","created":"%s"}\n' \
  "$(date --iso-8601=seconds)" > "$AUTOTEST_ROOT/AUTOTEST_STATUS.json"
printf '# CARLA Autotest Status\n\n- Plan: carla_aris_w15_20260817_130318\n- Root: `%s`\n- Status: TODO\n' \
  "$AUTOTEST_ROOT" > "$AUTOTEST_ROOT/AUTOTEST_STATUS.md"
: > "$AUTOTEST_ROOT/commands.log"
```

每个后续独立 shell 先执行：

```bash
AUTOTEST_ROOT="$(cat /home/yk/ws/carla_benchmark_data/AUTOTEST_CURRENT.txt)"
source "$AUTOTEST_ROOT/run.env"
source /opt/ros/humble/setup.bash
source /home/yk/ws/install/setup.bash 2>/dev/null || true
export HF_HOME=/home/yk/ws/.cache/huggingface
export TORCH_HOME=/home/yk/ws/.cache/torch
```

每个 run 必须产生 `$AUTOTEST_ROOT/run_records/<RUN_ID>.json`，至少包含：

```json
{
  "run_id": "P0-A-001",
  "depends_on": [],
  "status": "PASS|FAIL|BLOCKED|INVALID|NOT_RUN",
  "command": "exact command",
  "started_at": "ISO-8601",
  "ended_at": "ISO-8601",
  "exit_code": 0,
  "log": "absolute path",
  "outputs": ["absolute path"],
  "acceptance": {"gate": true, "details": {}}
}
```

`commands.log` 逐条追加 run ID、完整命令、起止时间和退出码。模型原生输出保留 `report.json`/CSV；汇总额外生成 strict JSON/CSV。不得只写自然语言结论。

## 3. 依赖与资源预算

### 必需依赖

- Ubuntu/Linux、ROS 2 Humble、colcon、Python 3.10；
- CARLA server/client 0.9.16，目录 `/home/yk/ws/third_party/CARLA_0.9.16`；
- NVIDIA driver/Vulkan、PyTorch CUDA；单 GPU；
- NumPy、SciPy、Pillow、PyYAML、torch、transformers 4.46.3、huggingface-hub 0.36.0、tokenizers 0.20.3；
- 本地已缓存上述 SegFormer revision；
- P0-B 三个旧数据目录当前均存在。

已归档 60–100 帧、五 offset 评测耗时约 17–23 秒（point-only）与 79 秒（turning voxel），硬件为 RTX 5070 Laptop GPU。以下预算同时考虑 CARLA 启停、采集、I/O、三 seed 和保守余量：

| 阶段 | 单 GPU 估算 | 磁盘增量估算 | 自动执行 |
| --- | ---: | ---: | --- |
| P0 sanity + baseline | 4–7 GPU·h，6–12 h wall | 8–15 GB | 是，硬门控制 |
| P1 main + ablation | 12–20 GPU·h，18–36 h wall | 25–45 GB | P0 全通过后是 |
| P2 3 Town × 3 weather × 3 seed | 24–40 GPU·h，2–4 天 wall | 35–55 GB | **否，仅估算** |

总自动预算上限建议为 27 GPU·h；超过估算 2× 的单 run 标记 `BLOCKED` 并停止该依赖链，不静默继续烧卡。

## 4. SANITY：P0-A 环境与代码硬门

### P0-A-001 preflight

```bash
set -o pipefail
cd /home/yk/ws/src/semantic_mapping
{
  date --iso-8601=seconds
  git status --short --branch
  git rev-parse HEAD
  git diff --stat
  git diff | sha256sum
  df -h /home/yk/ws
  nvidia-smi
  python3 -c "import carla; print(carla.__file__)"
  python3 -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
  test -f /home/yk/ws/third_party/CARLA_0.9.16/CarlaUE4.sh
  test -f /home/yk/ws/.cache/huggingface/hub/models--nvidia--segformer-b0-finetuned-cityscapes-1024-1024/snapshots/21b3847fae21ddee674abd31129307b6a1235bd9/pytorch_model.bin
} 2>&1 | tee "$AUTOTEST_ROOT/preflight/P0-A-001.log"
```

硬门：GPU/driver、PyTorch CUDA、CARLA import、server 文件、固定模型 snapshot、磁盘空间均可用；dirty 状态已保存。任何失败=`BLOCKED/FAIL`，停止。

### P0-A-002 build + directed tests

```bash
set -euo pipefail
cd /home/yk/ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install --packages-select semantic_mapping 2>&1 | tee "$AUTOTEST_ROOT/logs/P0-A-002-build.log"
source /home/yk/ws/install/setup.bash
cd /home/yk/ws/src/semantic_mapping
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest \
  test/test_carla_benchmark.py \
  test/test_carla_reliability.py \
  test/test_carla_reliability_capture.py \
  test/test_carla_evaluate_reliability.py \
  test/test_reliability_factors.py \
  test/test_reliability_voxel_ablation.py -q \
  2>&1 | tee "$AUTOTEST_ROOT/logs/P0-A-002-pytest.log"
ament_flake8 semantic_mapping/carla test/test_carla*.py \
  test/test_reliability_factors.py test/test_reliability_voxel_ablation.py \
  2>&1 | tee "$AUTOTEST_ROOT/logs/P0-A-002-flake8.log"
ament_pep257 semantic_mapping/carla \
  2>&1 | tee "$AUTOTEST_ROOT/logs/P0-A-002-pep257.log"
```

硬门：build、六个定向 test 文件、flake8、pep257 全部退出 0。

### P0-A-003 console entry help

```bash
set -euo pipefail
for tool in carla_capture_benchmark carla_evaluate_benchmark carla_capture_reliability carla_evaluate_reliability; do
  ros2 run semantic_mapping "$tool" --help
done 2>&1 | tee "$AUTOTEST_ROOT/logs/P0-A-003-entry-help.log"
```

硬门：四个入口全部退出 0。

## 5. BASELINE：P0-B 旧数据回归

三次 point-only 必须各自输出到独立目录；turning voxel 使用第四个目录，避免 non-empty output 阻断。

```bash
set -euo pipefail
declare -A DATASETS=(
  [stationary]=/home/yk/ws/carla_benchmark_data/reliability_baseline_fixed_20260813_155014
  [constant]=/home/yk/ws/carla_benchmark_data/motion_cv_constantmode_20260813_211718
  [turning]=/home/yk/ws/carla_benchmark_data/motion_turning_clean60_20260813_214753
)
for profile in stationary constant turning; do
  ros2 run semantic_mapping carla_evaluate_reliability \
    --dataset "${DATASETS[$profile]}" \
    --output "$AUTOTEST_ROOT/reliability/regression_${profile}_point" \
    --params-file /home/yk/ws/src/semantic_mapping/config/semantic_mapping_sim_livox.yaml \
    --segformer-model "$SEGFORMER_MODEL" --segformer-revision "$SEGFORMER_REVISION" \
    --time-offset-ms 0 20 50 100 150 --frame-tolerance-ms 6 \
    --device cuda --fp16 --max-points-per-frame 1000 \
    --posterior-storage none --no-voxel-eval
done
ros2 run semantic_mapping carla_evaluate_reliability \
  --dataset "${DATASETS[turning]}" \
  --output "$AUTOTEST_ROOT/reliability/regression_turning_voxel" \
  --params-file /home/yk/ws/src/semantic_mapping/config/semantic_mapping_sim_livox.yaml \
  --segformer-model "$SEGFORMER_MODEL" --segformer-revision "$SEGFORMER_REVISION" \
  --time-offset-ms 0 20 50 100 150 --frame-tolerance-ms 6 \
  --device cuda --fp16 --max-points-per-frame 1000 \
  --posterior-storage none --voxel-eval
```

硬门：manifest 事实与归档一致；accepted/sample flow/paired common points 一致；确定性指标绝对差 ≤1e-6；Motion V2 结论仍为机制响应通过、Voxel 主门失败。revision/manifest 不一致=`INVALID`。

## 6. SANITY：P0-C 30 帧双链路冒烟

### CARLA server 生命周期

先执行 `pgrep -af 'CarlaUE4-Linux-Shipping'` 与 `ss -ltnp | grep ':2000'`。已有可连接的 0.9.16 server 则复用；端口空闲才执行：

```bash
cd /home/yk/ws/third_party/CARLA_0.9.16
VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/nvidia_icd.json \
__NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia \
__VK_LAYER_NV_optimus=NVIDIA_only \
nohup ./CarlaUE4.sh -RenderOffScreen -nosound -quality-level=Low -carla-port=2000 \
  > "$AUTOTEST_ROOT/logs/carla_server.log" 2>&1 &
printf '%s\n' "$!" > "$AUTOTEST_ROOT/preflight/carla_server_owned.pid"
```

只可终止本 run 记录且身份仍匹配的 owned PID；复用的 server 不得终止。连接命令：

```bash
python3 -c "import carla;c=carla.Client('127.0.0.1',2000);c.set_timeout(10);print(c.get_client_version());print(c.get_server_version());print(c.get_world().get_map().name);print(c.get_available_maps())"
```

client/server 必须同为 0.9.16。非 CARLA 占端口=`BLOCKED`。

每次 capture 完成后、SegFormer evaluator 启动前，必须先释放 CARLA GPU。仅对 owned PID 使用：

```bash
pid_file="$AUTOTEST_ROOT/preflight/carla_server_owned.pid"
if test -f "$pid_file"; then
  pid="$(cat "$pid_file")"
  ps -p "$pid" -o args= | grep -q 'CarlaUE4-Linux-Shipping'
  kill "$pid"
  wait "$pid" 2>/dev/null || true
  rm -f "$pid_file"
fi
```

若复用了他人启动的 server 且无法由确认的 owner 正常停止，则在离线推理前标记 `BLOCKED`，不得自行杀进程，也不得让 CARLA 与 SegFormer 并发争用 8 GB 显存。image evaluator 完成后，重新执行同一 server 前检/启动流程，再采集 reliability smoke；reliability capture 后再次释放 owned server，才运行 reliability evaluator。

### P0-C image smoke

```bash
IMAGE_SMOKE="$AUTOTEST_ROOT/image/smoke_seed42"
ros2 run semantic_mapping carla_capture_benchmark \
  --host 127.0.0.1 --port 2000 --map Town05 --output "$IMAGE_SMOKE" \
  --cache-dir /home/yk/ws/.cache/carla --frames 30 --save-every 1 \
  --vehicles 12 --vehicle-classes car truck bus bicycle motorcycle \
  --width 640 --height 480 --grid-rows 4 --grid-cols 6 --seed 42 --stationary-ego
ros2 run semantic_mapping carla_evaluate_benchmark \
  --dataset "$IMAGE_SMOKE" --output "$IMAGE_SMOKE/results/segformer_smoke" \
  --backend segformer --segformer-model "$SEGFORMER_MODEL" \
  --segformer-revision "$SEGFORMER_REVISION" --device cuda --fp16 --save-overlay-every 1
```

硬门：format_version=2、frames 非空、manifest 实际地图 canonical name=Town05、exit 0；`report.json`、`per_frame.csv`、overlay 存在且 JSON 无 NaN/Infinity。

### P0-C reliability smoke

```bash
RELIABILITY_SMOKE="$AUTOTEST_ROOT/reliability/smoke_seed42"
ros2 run semantic_mapping carla_capture_reliability \
  --host 127.0.0.1 --port 2000 --traffic-manager-port 8000 \
  --town Town05 --weather ClearNoon --seed 42 --num-frames 30 --warmup-frames 10 \
  --output-dir "$RELIABILITY_SMOKE" --cache-dir /home/yk/ws/.cache/carla \
  --fixed-delta 0.01 --camera-tick 0.01 --lidar-tick 0.05 \
  --width 640 --height 480 --fov 90 --lidar-channels 64 --lidar-range 50 \
  --lidar-points-per-second 600000 --lidar-rotation-frequency 20 \
  --vehicles 12 --walkers 0 --vehicle-classes car truck bus bicycle motorcycle \
  --stationary-ego --no-moving-targets
ros2 run semantic_mapping carla_evaluate_reliability \
  --dataset "$RELIABILITY_SMOKE" --output "$AUTOTEST_ROOT/reliability/smoke_eval" \
  --params-file /home/yk/ws/src/semantic_mapping/config/semantic_mapping_sim_livox.yaml \
  --segformer-model "$SEGFORMER_MODEL" --segformer-revision "$SEGFORMER_REVISION" \
  --time-offset-ms 0 20 50 100 150 --device cuda --fp16 --voxel-eval
```

硬门：manifest format=`carla_reliability_v1`；所有 sensor/metadata 数量匹配；alignment aggregate ≥0.98、rejected=0、supported points>0；accuracy/ECE/NLL/Brier 有限；`per_point.csv`、`factor_summary.csv`、`report.json` 存在。

## 7. BASELINE：P0-D SegFormer 类别能力

对 seed 11/42/73 分别采集一次，再在同一数据上扫描固定阈值。runner 展开为 3 capture + 9 eval runs：

```bash
for seed in 11 42 73; do
  dataset="$AUTOTEST_ROOT/image/town05_clear_seed${seed}"
  ros2 run semantic_mapping carla_capture_benchmark \
    --host 127.0.0.1 --port 2000 --map Town05 --output "$dataset" \
    --cache-dir /home/yk/ws/.cache/carla --frames 400 --save-every 5 \
    --vehicles 40 --vehicle-classes car truck bus bicycle motorcycle \
    --walkers 25 --walker-min-distance 6 --walker-max-distance 35 \
    --width 640 --height 480 --grid-rows 4 --grid-cols 6 --seed "$seed" --stationary-ego
  for threshold in 0.35 0.45 0.55; do
    token="${threshold/./p}"
    ros2 run semantic_mapping carla_evaluate_benchmark \
      --dataset "$dataset" --output "$dataset/results/segformer_c${token}" \
      --backend segformer --segformer-model "$SEGFORMER_MODEL" \
      --segformer-revision "$SEGFORMER_REVISION" \
      --segformer-confidence "$threshold" --device cuda --fp16 --save-overlay-every 5
  done
done
```

实际执行需让 CARLA 采集与 SegFormer 推理串行：每个采集批次后停止 owned CARLA，再评测；下一 seed 再启动。硬门：format-v2、实际 Town05、`valid_for_vehicle_benchmark=true`、`missing_vehicle_classes=[]`、每类 ≥30 可用实例观测。模型门：road IoU≥0.70、vehicle IoU≥0.25、每类 detection/class accuracy 均≥0.70；任一类失败就明确 `FAIL`，不继续降阈值。walker actor 位置不声称 bitwise 重现。

## 8. BASELINE：P0-E 静态可靠性

```bash
for seed in 11 42 73; do
  dataset="$AUTOTEST_ROOT/reliability/static_town05_clear_seed${seed}"
  ros2 run semantic_mapping carla_capture_reliability \
    --host 127.0.0.1 --port 2000 --traffic-manager-port 8000 \
    --town Town05 --weather ClearNoon --seed "$seed" --num-frames 300 --warmup-frames 40 \
    --output-dir "$dataset" --cache-dir /home/yk/ws/.cache/carla \
    --fixed-delta 0.01 --camera-tick 0.01 --lidar-tick 0.05 \
    --width 640 --height 480 --fov 90 --lidar-channels 64 --lidar-range 50 \
    --lidar-points-per-second 600000 --lidar-rotation-frequency 20 \
    --vehicles 24 --walkers 0 --vehicle-classes car truck bus bicycle motorcycle \
    --stationary-ego --no-moving-targets
  ros2 run semantic_mapping carla_evaluate_reliability \
    --dataset "$dataset" --output "$AUTOTEST_ROOT/reliability/static_eval_seed${seed}" \
    --params-file /home/yk/ws/src/semantic_mapping/config/semantic_mapping_sim_livox.yaml \
    --segformer-model "$SEGFORMER_MODEL" --segformer-revision "$SEGFORMER_REVISION" \
    --time-offset-ms 0 --device cuda --fp16 --posterior-storage none --voxel-eval
done
```

硬门：alignment≥0.98、rejected=0、有限指标、GT/sample flow 完整。质量 claim：`r_semantic` correctness AUROC>0.5，至少 2/3 seed 同方向；cluster/bootstrap 95% CI 包含 0.5 时记“证据不足”。

## 9. MAIN：P1-F Range/Density/View/Semantic 单因素

### 9.1 YAML 副本生成命令

对每个 `{field,value}` 生成唯一副本；不得写正式 YAML：

```bash
python3 - "$field" "$value" "$yaml_copy" <<'PY'
import pathlib, sys, yaml
field, raw, target = sys.argv[1:]
source = pathlib.Path('/home/yk/ws/src/semantic_mapping/config/semantic_mapping_sim_livox.yaml')
doc = yaml.safe_load(source.read_text(encoding='utf-8'))
doc['ga_bsvm_node']['ros__parameters'][field] = float(raw)
path = pathlib.Path(target)
path.parent.mkdir(parents=True, exist_ok=True)
path.write_text(yaml.safe_dump(doc, sort_keys=False, allow_unicode=True), encoding='utf-8')
PY
```

矩阵：

| Run family | 自变量 | 值 | 数据 |
| --- | --- | --- | --- |
| P1-F-RANGE | `range_scale_m` | 10,20,30 | P0-E 三 seed |
| P1-F-DENSITY-W | `density_scale` | 4,8,16 | P0-E 三 seed |
| P1-F-VIEW | `view_edge_penalty` | 0,0.2,0.4 | P0-E 三 seed |
| P1-F-SEM-FLOOR | `semantic_confidence_floor` | 0.05,0.15,0.30 | P0-E 三 seed |
| P1-F-TEMP | evaluator `--segformer-temperature` | 0.75,1.0,1.25,1.5 | P0-E 三 seed |

每个矩阵单元的真实 evaluator 模板：

```bash
ros2 run semantic_mapping carla_evaluate_reliability \
  --dataset "$dataset" --output "$output" --params-file "$yaml_copy" \
  --segformer-model "$SEGFORMER_MODEL" --segformer-revision "$SEGFORMER_REVISION" \
  --segformer-temperature "$temperature" --time-offset-ms 0 \
  --device cuda --fp16 --posterior-storage none --voxel-eval
```

Density 还需要真实传感器控制：对 seed 11/42/73 分别新增 32/300000、16/150000 两组采集（其余完全同 P0-E），再用正式 YAML 副本、offset 0、`--voxel-eval` 评测；64/600000 复用 P0-E。

选择硬门：one-factor-at-a-time；先看 correctness AUROC/AURC/reliability gap，再看 VoxelMap；至少 2/3 seed 同方向、少数类 coverage 无明显下降。不能只凭 weighted accuracy 或公式单调选值。这里只产出 CARLA 候选，不写正式配置。

## 10. MAIN：P1-G Motion 与时间偏移

stationary 复用 P0-E 数据但重新以五 offset 评测。constant_velocity 与 turning 对三 seed 各采集：

```bash
ros2 run semantic_mapping carla_capture_reliability \
  --host 127.0.0.1 --port 2000 --traffic-manager-port 8000 \
  --town Town05 --weather ClearNoon --seed "$seed" --num-frames 300 --warmup-frames 40 \
  --output-dir "$dataset" --cache-dir /home/yk/ws/.cache/carla \
  --fixed-delta 0.01 --camera-tick 0.01 --lidar-tick 0.05 \
  --width 640 --height 480 --fov 90 --lidar-channels 64 --lidar-range 50 \
  --lidar-points-per-second 600000 --lidar-rotation-frequency 20 \
  --vehicles 24 --walkers 0 --vehicle-classes car truck bus bicycle motorcycle \
  --no-moving-targets --ego-motion-profile "$profile" \
  --ego-speed 5.0 --ego-throttle 0.45 --ego-steer 0.35
```

其中 `profile=constant_velocity|turning`。每个 stationary/constant/turning × seed 的评测命令：

```bash
ros2 run semantic_mapping carla_evaluate_reliability \
  --dataset "$dataset" --output "$output" \
  --params-file /home/yk/ws/src/semantic_mapping/config/semantic_mapping_sim_livox.yaml \
  --segformer-model "$SEGFORMER_MODEL" --segformer-revision "$SEGFORMER_REVISION" \
  --time-offset-ms 0 20 50 100 150 --frame-tolerance-ms 6 \
  --device cuda --fp16 --posterior-storage none --voxel-eval
```

必须报告 actual offset/timing error、相对转角/平移、V1 diagnostic、V2、paired common points、accuracy、AUROC/AURC 和 voxel。机制门与效果门分开。当前预注册结论是 Motion V2 未通过效果门；复现不允许通过扫 rotation/translation scale“调到通过”。

## 11. ABLATION：P1-H 六组 VoxelMap

`carla_evaluate_reliability --voxel-eval` 已在同一调用中生成六组：`none`、`semantic`、`semantic_range`、`semantic_range_density`、`semantic_range_density_view`、`full`。P1-H 不再二次推理；它读取 P0-E/P1-F/P1-G 的 `report.json.ablation.voxel_by_offset_ms`，生成：

- `$AUTOTEST_ROOT/summary/P1_H_voxel_ablation.csv`
- `$AUTOTEST_ROOT/summary/P1_H_voxel_ablation.json`
- `$AUTOTEST_ROOT/summary/P1_H_decision.md`

执行模型可在 `$AUTOTEST_ROOT/summary/aggregate_results.py` 实现输出本地汇总器并运行：

```bash
python3 "$AUTOTEST_ROOT/summary/aggregate_results.py" \
  --root "$AUTOTEST_ROOT" \
  --output-json "$AUTOTEST_ROOT/summary/P1_H_voxel_ablation.json" \
  --output-csv "$AUTOTEST_ROOT/summary/P1_H_voxel_ablation.csv"
```

该 helper 属于本次结果目录，不修改项目源码。硬门：all-GT accuracy 相对 none 下降≤1 pp；coverage 相对前一组下降≤0.5 pp；gap 或 AUROC 至少一个稳定改善且另一个不明显退化；≥2/3 seed 同方向；不得通过删 unsupported/困难点制造改善。

## 12. MAIN：P1-I 多 seed 统计与最终交付

输出本地汇总器以 seed/frame 为 cluster 做 bootstrap，不把点当独立重复。运行：

```bash
python3 "$AUTOTEST_ROOT/summary/aggregate_results.py" \
  --root "$AUTOTEST_ROOT" --bootstrap-seed 20260817 --bootstrap-replicates 2000 \
  --final-json "$AUTOTEST_ROOT/summary/FINAL_REPORT.json" \
  --final-csv "$AUTOTEST_ROOT/summary/run_metrics.csv" \
  --final-md "$AUTOTEST_ROOT/summary/FINAL_REPORT.md"
```

最终必须包含 commit/dirty、CARLA/GPU/model revision、逐 run 状态/命令/退出码/耗时/产物、GT alignment/sample flow、SegFormer 逐类、各 factor、六组 voxel、逐 seed 均值/标准差/95% CI、失败项、P2 预算、Gazebo/实机遗留。严格区分数据有效、模型通过、参数候选和可接入实机；不得自动改正式参数。

## 13. P2 HOLD（只估算，不启动）

计划矩阵：Town03/Town05/Town10HD_Opt × ClearNoon/CloudyNoon/WetNoon × seed 11/42/73，共 27 格；每格先 30 帧 smoke，再 300 帧。预计 24–40 GPU·h、2–4 天单 GPU wall、35–55 GB。还需 CLIP、CPU/CUDA、FP32/FP16、posterior temperature 曲线与长序列 VoxelMap 压测时另加约 8–15 GPU·h。

P2 的任何命令在 tracker 中保持 `TODO (HOLD_P2)`，等待用户另行确认；`AUTO_DEPLOY=true` 不适用于 P2。

## 14. 里程碑停止/继续规则

| Gate | 继续条件 | 失败动作 |
| --- | --- | --- |
| G0 P0-A | build/test/lint/entries/CARLA/CUDA/model cache 全通过 | 后续全部 NOT_RUN |
| G1 P0-B | 旧结果 deterministic 回归，Motion V2 结论未漂移 | 新采集 NOT_RUN |
| G2 P0-C | 双链路 30 帧 schema、GT alignment、strict outputs 全通过 | 正式矩阵 NOT_RUN |
| G3 P0-D/E | image support 有效；静态 GT/metrics 有效 | P1 NOT_RUN |
| G4 P1-F/G | 控制变量成立，三 seed 数据完整 | 执行已完成项汇总，不冻结参数 |
| G5 P1-H/I | 六组体素门与跨 seed 统计完成 | 报告 negative/inconclusive，不降门 |

## 15. 现有证据锚点

- `docs/results/carla_motion_v2_20260813/README.md`：Motion V2 机制通过、体素主门失败；
- 三个归档数据集 manifest 实际为 `Carla/Maps/Town10HD_Opt`，禁止按历史目录名写成 Town05；
- `semantic_mapping/carla/carla_evaluate_reliability.py`：strict output、GT fail-closed、固定采样、六组 voxel 与 non-empty output 拒绝；
- `semantic_mapping/carla/carla_evaluate_benchmark.py`：SegFormer threshold、revision、format-v2 instance scoring；
- `setup.py`：四个 ROS console entry 已注册。
