# Isolated smoke-test versions used on the ROS 2 Humble development PC.
transformers==4.46.3
huggingface-hub==0.36.0
tokenizers==0.20.3

# Reuse the platform-specific torch build. On Lite3, install the JetPack-
# compatible build and export the model to TensorRT instead of using this file.
